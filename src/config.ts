export const CONFIG = {
  // other options: "_id", "name"
  IDENTIFIER: "id",
  ORDERED_LIST: false,
};

export const algoVariation = {
  nested: false, // the "not so" efficient one
  eficient: true,
};
